# Wave-Management-Bot — Claude Code entry point

> ⚠️ **Single source of truth lives in [`AGENTS.md`](AGENTS.md)** at the repo root — the universal instructions file that *every* AI agent reads (Claude, Antigravity/Gemini, Cursor, Copilot, …).
>
> This file exists only so Claude Code loads that same content. **Do not duplicate instructions here — edit `AGENTS.md` instead.**

The line below imports the master file so Claude Code picks up the full project context:

@AGENTS.md
